-- Table: public."Users"

-- DROP TABLE public."Users";

CREATE TABLE public."Users"
(
    "Id" character varying(200) COLLATE pg_catalog."default" NOT NULL,
    "Email" character varying(256) COLLATE pg_catalog."default" NOT NULL,
    "PasswordHash" text COLLATE pg_catalog."default" NOT NULL,
    "PhoneNumber" text COLLATE pg_catalog."default",
    "FullName" text COLLATE pg_catalog."default" NOT NULL,
    "CreatedDate" timestamp(3) without time zone NOT NULL,
    "Birthday" timestamp(3) without time zone,
    "Gender" character varying(10) COLLATE pg_catalog."default",
    "Address" character varying(500) COLLATE pg_catalog."default",
    "Avatar" character varying(300) COLLATE pg_catalog."default",
    "LastLogin" timestamp(3) without time zone,
    "Status" character varying(50) COLLATE pg_catalog."default",
    "QRCode" character varying(100) COLLATE pg_catalog."default",
    "LoginFailedCount" numeric,
    CONSTRAINT users_pkey PRIMARY KEY ("Id")
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public."Users"
    OWNER to postgres;


-- Table: public."Roles"

-- DROP TABLE public."Roles";

CREATE TABLE public."Roles"
(
    "Id" character varying(200) COLLATE pg_catalog."default" NOT NULL,
    "Name" character varying(256) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT "Roles_pkey" PRIMARY KEY ("Id")
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public."Roles"
    OWNER to postgres;

-- Table: public."UserRoles"

-- DROP TABLE public."UserRoles";

CREATE TABLE public."UserRoles"
(
    "UserId" character varying(200) COLLATE pg_catalog."default" NOT NULL,
    "RoleId" character varying(200) COLLATE pg_catalog."default" NOT NULL,
    CONSTRAINT "UserRoles_pkey" PRIMARY KEY ("UserId", "RoleId")
)
WITH (
    OIDS = FALSE
)
TABLESPACE pg_default;

ALTER TABLE public."UserRoles"
    OWNER to postgres;